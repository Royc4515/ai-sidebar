# AI Sidebar
Chrome extension that injects a context-aware AI assistant sidebar into any webpage.
Supports six AI providers: Claude, Gemini, OpenAI, Grok, Groq, and Ollama — with streaming responses.
Load unpacked from `chrome://extensions` after cloning — no build step required.
